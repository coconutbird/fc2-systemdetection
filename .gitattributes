# Treat the carriage return in the repository's existing CRLF files as EOL,
# while retaining Git's standard trailing-whitespace checks.
* whitespace=blank-at-eol,blank-at-eof,space-before-tab,cr-at-eol
